import type { Config } from "tailwindcss"

const config: Config = {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}",
    "*.{js,ts,jsx,tsx,mdx}",
  ],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "hsl(var(--primary))",
          foreground: "hsl(var(--primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--secondary))",
          foreground: "hsl(var(--secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--destructive))",
          foreground: "hsl(var(--destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--muted))",
          foreground: "hsl(var(--muted-foreground))",
        },
        accent: {
          DEFAULT: "hsl(var(--accent))",
          foreground: "hsl(var(--accent-foreground))",
        },
        popover: {
          DEFAULT: "hsl(var(--popover))",
          foreground: "hsl(var(--popover-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--card))",
          foreground: "hsl(var(--card-foreground))",
        },
        // Custom PropertyEase colors
        "property-dark": "#1e293b",
        "property-purple": "#7c3aed",
        "property-pink": "#ec4899",
        "property-gradient-start": "#0f172a",
        "property-gradient-middle": "#581c87",
        "property-gradient-end": "#7c2d92",
        // PropertyEase Professional Color Palette
        "property-primary": "#1E2A38", // Navy Blue - Nav bar, buttons, icons
        "property-secondary": "#6C7A89", // Cool Gray - Accent buttons, highlights
        "property-background": "#F7F9FA", // Soft White - Main background
        "property-background-alt": "#EDEDED", // Light Gray - Alternative background
        "property-action": "#2F80ED", // Professional Blue - Add Property, primary actions
        "property-danger": "#D32F2F", // Subtle Red - Delete, warnings
        "property-card": "#FFFFFF", // White - Card backgrounds
        "property-card-alt": "#F9F9F9", // Very Light Gray - Alternative card background
        "property-text-primary": "#2C2C2C", // Charcoal Black - Headings
        "property-text-secondary": "#666666", // Gray - Subtext
      },
      borderRadius: {
        lg: "var(--radius)",
        md: "calc(var(--radius) - 2px)",
        sm: "calc(var(--radius) - 4px)",
      },
      keyframes: {
        "accordion-down": {
          from: { height: "0" },
          to: { height: "var(--radix-accordion-content-height)" },
        },
        "accordion-up": {
          from: { height: "var(--radix-accordion-content-height)" },
          to: { height: "0" },
        },
      },
      animation: {
        "accordion-down": "accordion-down 0.2s ease-out",
        "accordion-up": "accordion-up 0.2s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config

export default config
