{
  "buildCommand": "node -e \"const fs = require('fs'); const path = '.next'; if (fs.existsSync(path)) fs.rmSync(path, {recursive: true, force: true});\" && pnpm run build",
  "framework": "nextjs"
}
